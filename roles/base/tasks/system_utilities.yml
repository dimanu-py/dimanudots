---
# Base system utilities installation
# Installs essential system monitoring, file management, and disk usage tools

- name: Update pacman cache
  pacman:
    update_cache: yes
  tags: base, system

- name: Install system utilities tools
  pacman:
    name: "{{ base_system_packages | map(attribute='name') | list }}"
    state: present
  tags: base, system_utilities

