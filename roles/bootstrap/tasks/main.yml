---
# Bootstrap role main tasks
# System preparation, yay installation, and Ansible collections

- name: Import system preparation tasks
  include_tasks: system.yml
  tags: bootstrap, system

- name: Import yay installation tasks
  include_tasks: yay.yml
  tags: bootstrap, yay

- name: Import Ansible collections setup tasks
  include_tasks: collections.yml
  tags: bootstrap, collections