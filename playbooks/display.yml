---
# Phase 4: Display & Desktop Foundation playbook
# Sets up SDDM, fonts, themes, and visual foundation

- name: Phase 4 - Display & Desktop Foundation
  hosts: localhost
  become: true
  gather_facts: yes
  
  tasks:
    - name: Import display foundation setup
      include_tasks: ../tasks/display_foundation.yml
      tags:
        - display
        - sddm
        - fonts
        - themes

  handlers:
    # No handlers needed - all restarts handled inline